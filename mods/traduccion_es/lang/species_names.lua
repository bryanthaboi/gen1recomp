-- Species names
--
-- Filled automatically from the EUR dump (the Spanish release kept the
-- English species names, so in practice nothing changes). An entry here
-- wins over the automatic layer.

return {
}
